# SAM 3.1 multiplex concept-video tracker. Concepts come from prompts.yaml
# (primary_taxonomy); the checkpoint is fetched from facebook/sam3.1.
segmentation:
  version: "sam3.1"
  use_fa3: false           # FlashAttention 3 (GPU-only); off by default
  output_prob_thresh: 0.5  # SAM 3.1 mask probability threshold

depth:
  enabled: true
  model: "depth-anything-v2-large"
  min_lidar_anchors: 30
  ransac_n_iter: 200
  ransac_inlier_threshold_m: 0.5
  use_static_anchors_only: true
  fallback_window: 5
  sky_mask_top_fraction: 0.3
  output_dtype: "float16"

reid:
  model: "dinov2_vitl14"
  every_k_frames: 5

cross_cam:
  match_radius_m: 1.5

prompts_path: "/config/prompts.yaml"
