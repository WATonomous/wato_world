name: build-base-images

on:
  push:
    branches: [main]
    paths:
      - "docker/base/**"
      - ".github/workflows/build_base_images.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-base:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: cuda
            dockerfile: docker/base/inject_cuda_base.Dockerfile
            tag: cuda-12.4
          - flavor: cpu
            dockerfile: docker/base/inject_cpu_base.Dockerfile
            tag: cpu-ubuntu24.04
    steps:
      - uses: actions/checkout@v4

      - id: repo
        run: echo "lower=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ghcr.io/${{ steps.repo.outputs.lower }}/base:${{ matrix.tag }}
          # OCI source label auto-links the ghcr package to this repo, which is
          # what grants the build_and_test workflow's GITHUB_TOKEN
          # (packages: read) permission to pull these bases. Without this, the
          # package exists "orphaned" and downstream workflows hit
          # 403 Forbidden on the anonymous token endpoint.
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
          provenance: false
          cache-from: type=gha,scope=base-${{ matrix.flavor }}
          cache-to: type=gha,scope=base-${{ matrix.flavor }},mode=max
